<?xml version="1.0" encoding="UTF-8"?>
<network_review_triplet>
    <description>Security improvement for network configuration</description>
    <before_config>server {
    listen 80;
    server_name example.com;
    location / {
        proxy_pass http://backend;
    }
}</before_config>
    <review_comment>Consider adding SSL/TLS configuration and security headers for production deployment</review_comment>
    <after_config>server {
    listen 443 ssl http2;
    listen 80;
    server_name example.com;
    
    ssl_certificate /etc/ssl/certs/example.com.crt;
    ssl_certificate_key /etc/ssl/private/example.com.key;
    
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    
    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    if ($scheme != "https") {
        return 301 https://$host$request_uri;
    }
}</after_config>
    <metadata>
        <reviewer>security-team</reviewer>
        <priority>high</priority>
        <category>security</category>
        <component>nginx</component>
    </metadata>
</network_review_triplet>